{
  "name": "spectraltable",
  "version": "0.1.0",
  "type": "module",
  "description": "GPU-accelerated spectral synthesis in the browser",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
